{
  "name": "ppplus",
  "version": "1.0.0",
  "private": true,
  "description": "PP+ Web App",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node frontend/server.js",
    "seed": "node frontend/seed-mysql.js"
  }
}
